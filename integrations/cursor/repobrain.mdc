---
description: RepoBrain session lifecycle — auto-inject repo memory, run local detection at phase boundaries, capture candidates for review
globs:
alwaysApply: true
---

# RepoBrain 会话规则（Cursor）

## 命令调用（开发态回退策略）

所有 `brain <args>` 调用遵循以下优先级：

1. `brain <args>` — 全局安装可用时直接使用
2. `npx brain <args>` — 全局不可用时通过 npx 调用
3. `node dist/cli.js <args>` — 仅当本仓库是 RepoBrain 自身开发仓库且 `dist/cli.js` 文件存在时使用

判断方法：执行命令前先尝试 `brain --version`，成功则用路径 1；失败则尝试 `npx brain --version`，成功则用路径 2；若当前仓库根目录存在 `package.json` 且 `name` 为 `"brain"` 且 `dist/cli.js` 存在，则用路径 3。

后续文档中所有 `brain <子命令>` 均表示按上述优先级解析后的实际命令。发布后的用户仓库通常只会命中路径 1。

## 会话开始时（必须）

每次新会话开始时，在回复用户之前先执行：

```bash
brain start --format json --task "<当前任务描述>"
```

将 `context_markdown` 作为本仓库背景上下文，`skill_plan` 作为路由参考；如果返回 `navigation_plan`，必须读取 canonical source paths、live checks 和 next steps。如果 `.brain/` 不存在，跳过并提示用户运行 `brain setup`。

无法使用 `brain start` 时，回退到：

```bash
brain inject
```

如果同一个 session 里后续又开了一个新 conversation，优先执行：

```bash
brain conversation-start --format json --task "<当前任务描述>" --path <已变更路径>
```

让 RepoBrain 根据 session 上下文决定是 `start`、`inject` 还是 `skip`；只有在明确只需要轻量 durable context 时，才直接执行 `brain inject`。

## 任务已知时

当任务变得更明确、需要路由决策时：

```bash
brain suggest-skills --format json --task "<当前任务描述>"
```

消费 `invocation_plan` 做路由；消费任何返回的 `navigation_plan` 来发现 canonical sources、live checks 和 next steps。不要在 Cursor 内创建独立的知识副本（如 .cursor/memories/ 等）。

## 阶段完成时运行本地检测

以下场景出现时，**优先运行本地检测命令**，而不是仅凭主观判断提议提取：

**强信号（识别到后应立即运行检测）：**
- 用户明确表示阶段结束："好了 / 先这样 / 这一版可以 / 继续下一个 / move on / ship it / ready for review"
- 你刚输出了汇总性完成语句："已修复 / 已实现 / 已完成 / implementation complete / all tests passing"
- 测试状态从失败变为成功
- 变更范围达到阈值：diff 超过 30 行 或 涉及 4+ 个文件

**需要和内容价值组合判断的场景：**
- 修复了重复出现的 bug
- 完成了一个子模块实现
- 跨多个文件 / 多个模块做了重构

**不应触发检测的弱信号（避免误触发）：**
- 用户只说"好的 / 谢谢 / ok / thanks / 嗯 / 收到"等简单确认词
- 没有配合任何实质变更或有价值的经验描述

### 构造提取输入

`brain capture` 依赖 stdin 获取会话经验摘要。**必须通过管道传入结构化内容**，否则本地启发式提取器无法识别有效信号。

每条经验用 **类型前缀** 开头，格式为 `<type>: <内容>`，其中 type 为 `decision` / `gotcha` / `convention` / `pattern` / `working` / `goal` 之一。内容应包含 **原因或约束**（如"因为…"、"避免…"、"否则…"），而非仅描述做了什么。

**模板：**

```bash
echo "<session_summary>" | brain capture --task "<任务描述>" --path <变更路径>
```

其中 `<session_summary>` 由你根据当前会话实际产生的经验组装，规则如下：

1. 回顾本阶段的关键决策、踩坑、约定、模式、临时上下文
2. 每条用对应类型前缀开头，一条一行
3. 如果有多条经验，用换行分隔
4. **不要编造经验**——只提取会话中实际发生的、对未来有帮助的知识
5. 如果回顾后确实没有值得保存的经验，不要调用 capture

**示例（假设会话中实际讨论了这些内容）：**

```bash
echo "decision: 选择 pnpm workspace 管理 monorepo，因为 npm 在 hoist 时会导致幽灵依赖
gotcha: tsconfig paths 必须和 package.json exports 对齐，否则运行时解析失败
convention: 新模块统一放在 packages/ 下，入口文件命名为 index.ts" | brain capture --task "搭建 monorepo 基础结构" --path packages/
```

`brain capture` 内部使用"阶段完成"信号作为 confidence booster，与内容价值信号共同决定是否提取。即使检测到阶段完成，如果没有值得保留的经验（如只修了 typo、只加了 debug log），`should_extract` 仍会返回 `false`。

`should_extract=true` 时默认生成 candidate（不是直接写 active）。`should_extract=false` 时不要打扰用户。

**同一轮对话中不要重复提出相同的 capture 建议**，除非变更范围或任务状态发生了显著变化。

## 失败路径

当任务违反了已知 memory 或重复了旧的失败模式时：

```bash
brain reinforce
```

## 临时上下文（working 类型）

只在本次会话有用的上下文，用 working 类型存储（7 天自动过期）：

```bash
echo "working: <临时上下文描述，包含原因或背景>" | brain extract --type working
```

## 跨会话目标（goal 类型）

用户描述的多会话任务，用 goal 类型记录：

```bash
echo "goal: <目标描述>，因为<原因>

进度：
- [ ] 步骤一" | brain extract --type goal
```

`brain inject` 会在每次会话开始时优先展示所有 active goal，自动恢复上下文。

## Routing feedback（本地策略闭环，非运行时编排）

契约：`integrations/contracts/routing-feedback.event.json`。将 JSON 数组或 NDJSON 通过 `stdin` 传给 `brain routing-feedback`（可加 `--json` 查看结构化结果）。RepoBrain 不接管 agent 执行，只记录结果以更新 preference candidate / 置信度 / 提醒队列。

- 用户明确说流程太繁琐、下次别这样 → 发送 `workflow_too_heavy` 或带 `skill`/`workflow` 的负反馈事件。
- 明显未按 `invocation_plan` 使用某 skill → 发送 `skill_ignored`，附上 `skill` 与说明。
- 任务成功且用户认可本次路由 → 发送 `skill_followed` 或 `workflow_success`。

用 `brain routing-feedback --explain <skill>` 查看该 skill 的 preference 与日志；`brain reinforce --pending` 会打印与 reinforce 队列一并保存的 routing 提醒，处理完后可用 `brain routing-feedback --ack-reminders` 清空。

## 规则

- `.brain/` 是唯一的持久知识存储，不要创建第二套 durable memory store。
- 不要在 Cursor 内创建独立的知识副本（如自行创建 .cursor/memories/ 等）。
- `brain start` / `brain inject` 提供上下文，`brain suggest-skills` 提供路由。
- `brain capture` 和 `brain extract` 是提取路径，`brain reinforce` 是失败路径。
- 默认 candidate-first：提取结果先存为 candidate，由用户 review 后 approve。
- 不要直接写入 `.brain/`。
